{"name":"Applica aggiornamento","on":{"workflow_dispatch":{}},"permissions":{"contents":"write"},"jobs":{"applica":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4"},{"run":"cat pezzo-* > agg.gz && gunzip -c agg.gz > aggiorna.patch && git apply aggiorna.patch && rm -f pezzo-* agg.gz aggiorna.patch && git config user.name mealup-bot && git config user.email mealup@users.noreply.github.com && git add -A && git commit -m 'MealUp: aggiornamento applicato' && git push"}]}}}
